#User function Template for python3

class Solution:
    def arraySortedOrNot(self, arr, n):
        for a in range(0, len(arr)-1):
            if arr[a] > arr[a+1]:
                return 0
        return 1

